<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <androidx.camera.view.PreviewView
        android:id="@+id/previewView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constrainedHeight="true"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/bottomBar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- 右上角：鏡頭／比例／相簿／設定，固定在畫面最上方，不受預覽比例影響 -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="16dp">

        <Button
            android:id="@+id/lensMenuButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/OverlayButton"
            android:layout_marginEnd="8dp"
            android:text="鏡頭" />

        <Button
            android:id="@+id/aspectRatioMenuButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/OverlayButton"
            android:layout_marginEnd="8dp"
            android:text="比例" />

        <Button
            android:id="@+id/galleryButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/OverlayButton"
            android:layout_marginEnd="8dp"
            android:text="相簿" />

        <Button
            android:id="@+id/settingsButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/OverlayButton"
            android:text="設定" />

    </LinearLayout>

    <!-- 錄影中計時提示，預設隱藏，固定在畫面最上方 -->
    <TextView
        android:id="@+id/recordingTimerText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="#CCD32F2F"
        android:paddingHorizontal="12dp"
        android:paddingVertical="6dp"
        android:text="● 00:00"
        android:textColor="#FFFFFF"
        android:textSize="13sp"
        android:textStyle="bold"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp" />

    <!-- 即時 GPS 資訊，顯示於預覽右下角：地址／經緯度／自訂文字／時間 -->
    <TextView
        android:id="@+id/gpsOverlayText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintWidth_max="280dp"
        android:background="#88000000"
        android:padding="8dp"
        android:textColor="#FFFFFF"
        android:textSize="12sp"
        android:gravity="end"
        android:text="定位中..."
        app:layout_constraintBottom_toBottomOf="@id/previewView"
        app:layout_constraintEnd_toEndOf="@id/previewView"
        android:layout_marginEnd="12dp"
        android:layout_marginBottom="12dp" />

    <!-- 底部操作區 -->
    <LinearLayout
        android:id="@+id/bottomBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="#111111"
        android:paddingHorizontal="20dp"
        android:paddingTop="8dp"
        android:paddingBottom="18dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- 亮度：按下彈出選單 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginBottom="8dp">

            <Button
                android:id="@+id/brightnessMenuButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:backgroundTint="#333333"
                android:textColor="#FFFFFF"
                android:textSize="13sp"
                android:text="亮度" />

        </LinearLayout>

        <!-- 模式切換／拍照按鈕：並排 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <Button
                android:id="@+id/modeToggleButton"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:backgroundTint="#333333"
                android:textColor="#FFFFFF"
                android:textSize="13sp"
                android:text="模式：拍照" />

            <Button
                android:id="@+id/captureButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="12dp"
                android:paddingVertical="12dp"
                android:text="拍照"
                android:textSize="18sp" />

        </LinearLayout>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
